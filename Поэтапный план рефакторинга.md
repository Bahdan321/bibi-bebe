## 📋 Поэтапный план рефакторинга
### Этап 1: Создание базовых компонентов (Приоритет: Высокий) 1.1 Создать универсальный компонент CustomButton
```
// components/base/CustomButton.tsx
interface CustomButtonProps {
  variant: 'primary' | 'secondary' | 
  'service' | 'round' | 'text';
  title: string;
  onPress: () => void;
  icon?: string;
  disabled?: boolean;
  loading?: boolean;
  size?: 'small' | 'medium' | 'large';
  style?: ViewStyle;
}
``` 1.2 Создать компонент CustomView
```
// components/base/CustomView.tsx
interface CustomViewProps {
  variant?: 'card' | 'container' | 'row' | 
  'column';
  padding?: number | 'small' | 'medium' | 
  'large';
  margin?: number | 'small' | 'medium' | 
  'large';
  backgroundColor?: string;
  borderRadius?: number;
  children: React.ReactNode;
}
``` 1.3 Создать компонент CustomTouchable
```
// components/base/CustomTouchable.tsx
interface CustomTouchableProps {
  onPress: () => void;
  disabled?: boolean;
  style?: ViewStyle;
  activeOpacity?: number;
  children: React.ReactNode;
}
```
### Этап 2: Рефакторинг существующих компонентов (Приоритет: Высокий) 2.1 Исправить TimePicker.tsx
- Заменить все Text на CustomText
- Исправить типизацию ScrollView refs
- Добавить правильные типы для event handlers
- Использовать CustomButton для кнопки подтверждения 2.2 Унифицировать компоненты кнопок
- Рефакторить Button.tsx для использования CustomButton
- Рефакторить ServiceButton.tsx для использования CustomButton
- Рефакторить RoundButton.tsx для использования CustomButton
- Рефакторить ClickableText.tsx для использования CustomButton 2.3 Обновить компоненты форм
- SettingsRow.tsx : заменить Text на CustomText
- TextInputField.tsx : создать единый стиль для всех полей ввода
### Этап 3: Рефакторинг экранов (Приоритет: Средний) 3.1 Обновить экраны аутентификации
- signIn.tsx : заменить все Text на CustomText
- signUp.tsx : заменить все Text на CustomText
- Использовать унифицированные компоненты кнопок 3.2 Обновить экраны настроек
- settings.tsx : заменить все Text на CustomText
- Использовать CustomView для контейнеров
### Этап 4: Создание системы дизайна (Приоритет: Средний) 4.1 Создать файл констант дизайна
```
// constants/design.ts
export const SPACING = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export const FONT_SIZES = {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 18,
  xl: 24,
};

export const BORDER_RADIUS = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
};
``` 4.2 Обновить ThemeProvider
- Добавить типизированные константы дизайна
- Создать предустановленные стили для компонентов
### Этап 5: Оптимизация и очистка (Приоритет: Низкий) 5.1 Удалить дублирующиеся компоненты
- После рефакторинга удалить старые версии компонентов кнопок
- Очистить неиспользуемые стили 5.2 Создать Storybook или документацию
- Документировать все базовые компоненты
- Создать примеры использования
## 🎯 Ожидаемые результаты
### Краткосрочные (после этапов 1-2)
- Единообразное использование CustomText во всех компонентах
- Исправленные ошибки TypeScript в TimePicker
- Унифицированная система кнопок
### Долгосрочные (после всех этапов)
- Сокращение кода на 30-40% за счет переиспользования
- Упрощение изменений дизайна - изменения в одном месте применяются везде
- Улучшенная типизация и меньше ошибок
- Консистентный UI во всем приложении
- Упрощенная поддержка и добавление новых функций
## 🚀 Рекомендации по реализации
1. Начать с этапа 1 - создание базовых компонентов
2. Тестировать каждый этап перед переходом к следующему
3. Использовать feature branches для каждого компонента
4. Обновлять документацию по мере рефакторинга
5. Проводить code review для каждого изменения
Этот план позволит поэтапно улучшить архитектуру приложения, достигнув максимального переиспользования компонентов и упростив поддержку кода в будущем.